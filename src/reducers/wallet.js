const INITIAL_STATE = {
  currencies: [],
  expenses: [],
  totalBRL: 0,
};

const wallet = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default wallet;
